import { UnicodeEmojiPipe } from './unicode-emoji-pipe';

describe('UnicodeEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new UnicodeEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
